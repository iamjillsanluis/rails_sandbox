# README

## Learn Maintenance Tasks
Redis dependency
```shell
brew install redis
brew services start redis
```

```shell
bin/rails db:seed

# run in a terminal
sidekiq

# run in a terminal
bin/rails server
```

